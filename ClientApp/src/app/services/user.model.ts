//Optional but recommended, once I figure out what data to model:
//model my data as a typescript interface


export interface User{
    uid: string;
    email: string;
    photoURL?: string;
    displayName?: string;
}
